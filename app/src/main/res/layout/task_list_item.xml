<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <TextView
        android:id="@+id/tv_task_name"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <ImageView
        android:id="@+id/iv_task_status"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintRight_toRightOf="@+id/tv_task_name"
        app:layout_constraintTop_toTopOf="@+id/tv_task_name"
        app:layout_constraintBottom_toBottomOf="@+id/tv_task_name"
        android:background="@drawable/ic_task_todo"/>

</androidx.constraintlayout.widget.ConstraintLayout>